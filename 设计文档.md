# AI 驱动学习资源站重构设计文档

## 1. 文档目标

本文档用于说明 AI 驱动学习资源站的重构目标、系统边界、核心架构、关键数据模型与分阶段落地方案，作为后续开发、分工和验收的统一依据。

本文档关注的是“系统设计”，不是具体实现教程。实现细节应以下列设计原则为准：

- 先完成最小可用闭环，再逐步引入 Agent、RAG 和自动审核能力。
- 优先兼容现有项目形态，避免为追求复杂架构而增加维护成本。
- 所有核心能力都应能拆分为可独立验证的模块。

## 2. 项目背景

当前学习资源往往分散在课程群、GitHub 仓库、课程网站、个人笔记和公开视频中，存在以下问题：

- 资源来源分散，难以系统化整理。
- 学习路径不清晰，前置知识关系靠个人经验判断。
- 资料很多，但缺少针对具体学习目标的检索与推荐能力。
- 新增资源的质量参差不齐，人工维护成本高。

本项目希望围绕计算机与通信相关课程，构建一个面向校内同学的学习资源平台。平台既能展示结构化课程路线图，也能通过 AI 能力辅助资源检索、目标拆解与内容审核。

## 3. 设计目标与范围

### 3.1 总体目标

构建一个面向计算机与通信专业学生的 AI 自学平台，核心能力包括：

- 基于课程前置关系的动态学习路线图。
- 面向站内资源的检索与问答能力。
- 支持用户提交资源并进入审核流程。
- 支持根据用户目标生成个性化学习路径建议。

### 3.2 第一阶段目标

第一阶段只解决“能用”和“能演示”的问题，聚焦以下闭环：

- 平台能够展示课程节点与前置关系。
- 平台能够管理课程资源与资源元数据。
- 用户能够输入学习目标并获得一条结构化学习路径。
- 用户能够对站内资料进行检索并获得基于资料的回答。
- 管理员能够审核新增资源。

### 3.3 暂不纳入第一阶段

以下能力有价值，但不应在第一阶段强行上线：

- 多 Agent 复杂协同编排。
- 全自动互联网大规模爬取。
- 复杂权限体系与多组织协作。
- 高复杂度推荐算法。
- 本地大模型部署优化。

## 4. 用户与典型场景

### 4.1 目标用户

- 计算机、通信及相关专业的本科生。
- 需要补前置知识的跨方向学习者。
- 维护课程资源的管理员或项目成员。

### 4.2 核心场景

#### 场景 A：查看学习路线图

用户进入平台后，可查看课程节点、课程简介、前置依赖以及相关资源，并根据自己的学习进度判断下一步学习内容。

#### 场景 B：按目标生成学习路径

用户输入目标，例如“暑假补完数字信号处理前置知识”或“准备做一个通信方向课程设计”，系统返回一条结构化学习路径，包含推荐课程、顺序与资源。

#### 场景 C：资料检索与问答

用户输入问题，例如“DSP 课程实验前需要掌握哪些知识”或“信号与系统相关实验资料有哪些”，系统基于站内知识库返回检索结果与总结答案。

#### 场景 D：用户提交新资源

用户提交外部链接或资料说明后，系统对内容进行预处理和元数据提取，管理员审核通过后入库。

## 5. 系统边界

### 5.1 系统负责的内容

- 课程节点、前置关系和学习路径管理。
- 资源元数据管理与审核。
- 站内资料的向量检索与问答。
- 学习目标到路线图的结构化生成。

### 5.2 系统不负责的内容

- 替代完整的 LMS 或教学系统。
- 提供所有资源的原始托管能力。
- 对外部网页的长期稳定镜像存储。
- 对回答结果做绝对正确性保证。

## 6. 总体架构

系统采用前后端分离架构，核心由四层组成：

1. 表现层：负责路线图展示、对话交互、资源浏览和管理端审核。
2. 应用层：负责认证、业务接口、权限判断与流程编排。
3. 智能层：负责学习路径生成、资源元数据提取、RAG 检索问答。
4. 数据层：负责结构化数据、文档切片、向量索引与缓存。

### 6.1 架构拆分

#### 前端

- 技术建议：React + TypeScript。
- 路线图渲染：React Flow。
- UI 组件：Tailwind CSS + shadcn/ui。
- 重构策略：采用渐进式重构，允许旧页面与新页面短期并存。
- 交互模块：
  - Workspace 主页面。
  - 学习路线图页。
  - 资源检索与问答页。
  - 资源详情侧边栏或弹窗。
  - 管理审核页。

前端形态建议如下：

- 主入口采用 Workspace 布局。
- 左侧用于目标输入、对话流或操作流。
- 右侧用于展示 Roadmap、资源详情和生成结果。
- 节点详情优先使用右侧固定面板或 Drawer 展示。

前端实施原则如下：

- 已有页面中可复用的数据录入和展示逻辑可暂时保留。
- 新增核心页面优先使用新的视觉和交互方案。
- 第一阶段不要求为了统一风格而重写全部旧组件。

#### 后端

- 技术建议：FastAPI。
- 职责：
  - 提供 REST API。
  - 组织学习路径生成流程。
  - 提供资源检索、问答与审核接口。
  - 负责文档处理任务和异步任务调度。

#### 智能服务

- 初期建议以“单服务 + 明确函数边界”为主，不强依赖 LangGraph。
- 在以下场景中引入 LLM：
  - 学习目标解析。
  - 路线图 JSON 生成。
  - 资源元数据提取。
  - RAG 回答生成。
- 当流程复杂度明显升高时，再考虑将生成、审核、检索流程升级为图编排。

#### 数据层

- 结构化数据库：PostgreSQL。
- 向量能力：pgvector。
- 缓存：Redis。
- 文档对象存储可后置引入，第一阶段不强制要求。

## 7. 核心业务设计

### 7.1 学习路线图模块

学习路线图模块用于表达课程节点之间的前置关系，并承载每个节点的资源集合。

#### 输入

- 用户当前目标。
- 用户已掌握课程或已学模块。
- 平台内预定义的课程图谱与资源数据。

#### 输出

- 节点列表。
- 边列表。
- 每个节点的元数据，例如名称、难度、简介、资源数量、是否已完成。

#### 关键设计点

- 路线图中的课程节点应以“课程或能力模块”为基本粒度，不宜过细到单一知识点。
- 前置关系应由人工维护为主，AI 生成结果只作为辅助建议。
- 前端渲染所需的节点和边结构应由后端统一生成，避免前端拼装业务逻辑。

### 7.2 学习路径生成模块

该模块负责把用户自然语言目标转换为平台可渲染的学习路径。

#### 处理流程

1. 解析用户目标，提取方向、时间范围、基础水平和偏好。
2. 从课程图谱中筛选候选节点。
3. 按前置关系补全缺失基础。
4. 调用 LLM 输出结构化学习路径。
5. 校验返回结果是否满足预定义 JSON Schema。
6. 返回前端渲染。

#### 设计约束

- LLM 只负责“建议”和“排序”，不直接决定底层课程依赖关系。
- 所有输出必须经过 Schema 校验，不允许裸文本直接驱动前端。
- 当生成失败时，系统必须回退到规则模板路径，而不是返回空白结果。

### 7.3 资源管理与审核模块

该模块负责新增资源的接入、标准化和人工审核。

#### 流程

1. 用户提交链接或资源描述。
2. 系统抓取网页标题、正文摘要、来源和可访问性信息。
3. LLM 提取元数据，例如分类、适用课程、难度、资源类型。
4. 系统将资源标记为“待审核”。
5. 管理员查看预处理结果并决定通过、驳回或修改后通过。
6. 审核通过后写入资源库，并触发向量化流程。

#### 设计原则

- AI 预审只降低人工成本，不能替代人工最终确认。
- 所有资源必须保留来源链接和抓取时间。
- 审核记录应可追踪，便于后续修订。

### 7.4 RAG 检索问答模块

该模块负责对站内资料进行检索、召回和答案组织。

#### 数据来源

- 培养方案文档。
- 课程实验指导。
- 项目仓库说明文档。
- 平台内人工整理的学习笔记与资源说明。

#### 处理流程

1. 用户输入问题。
2. 系统进行问题改写或关键词提取。
3. 在向量库和结构化资源中召回候选内容。
4. 结合资源元数据做重排。
5. 将检索片段与问题输入 LLM，生成带来源的回答。

#### 设计约束

- 回答必须附带来源资源，避免“无依据回答”。
- 若召回结果不足，应明确提示资料不足，而不是强行回答。
- 初期只面向站内资料，不直接做全网搜索问答。

## 8. 数据模型设计

第一阶段建议至少包含以下核心实体。

### 8.1 User

用于保存用户基本信息、角色和登录信息。

关键字段建议：

- `id`
- `email`
- `name`
- `role`
- `created_at`

### 8.2 CourseNode

用于描述课程或能力模块。

关键字段建议：

- `id`
- `title`
- `slug`
- `summary`
- `difficulty`
- `category`
- `is_active`
- `created_at`

### 8.3 CourseEdge

用于描述课程节点之间的前置关系。

关键字段建议：

- `id`
- `source_node_id`
- `target_node_id`
- `relation_type`
- `created_at`

### 8.4 Resource

用于保存资源元数据。

关键字段建议：

- `id`
- `title`
- `url`
- `resource_type`
- `source`
- `summary`
- `difficulty`
- `status`
- `submitted_by`
- `reviewed_by`
- `created_at`

### 8.5 ResourceCourseMapping

用于建立资源与课程节点的多对多关系。

关键字段建议：

- `id`
- `resource_id`
- `course_node_id`

### 8.6 DocumentChunk

用于保存文档切片与向量化前的文本单元。

关键字段建议：

- `id`
- `resource_id`
- `chunk_index`
- `content`
- `token_count`

### 8.7 Embedding

用于保存向量信息。

关键字段建议：

- `id`
- `chunk_id`
- `embedding`
- `model_name`
- `created_at`

### 8.8 InvitationCode

如果平台采用邀约制，可以保留邀请码表。

关键字段建议：

- `id`
- `code`
- `status`
- `expired_at`
- `used_by`

## 9. 接口设计建议

第一阶段建议优先定义以下接口。

### 9.1 路线图相关

- `GET /api/course-nodes`
- `GET /api/roadmaps/default`
- `POST /api/roadmaps/generate`

### 9.2 资源相关

- `GET /api/resources`
- `GET /api/resources/{id}`
- `POST /api/resources/submit`
- `POST /api/resources/{id}/review`

### 9.3 RAG 相关

- `POST /api/search`
- `POST /api/chat/retrieve`

### 9.4 管理与认证相关

- `POST /api/auth/login`
- `POST /api/auth/invitation/verify`

接口层应保证以下原则：

- 前后端通信数据结构稳定。
- 生成型接口必须有明确的输入输出 Schema。
- 审核与管理类接口必须保留操作人信息。

## 10. 非功能设计

### 10.1 可维护性

- 业务规则优先落在后端，不散落在前端组件中。
- LLM 提示词、Schema 和业务逻辑要分开管理。
- 检索、解析、生成流程要支持独立测试。

### 10.2 可扩展性

- 课程图谱、资源系统和问答系统应解耦。
- RAG 相关模块应支持替换 embedding 模型和重排策略。
- 资源抓取流程应支持后续接入异步队列。

### 10.3 性能

- 路线图接口应返回预聚合结果，避免前端多次拼接。
- 检索接口应控制召回数量和上下文长度。
- 文档解析和向量化应走异步任务，不阻塞主请求。

### 10.4 可靠性

- 外部网页抓取失败时应允许人工补录。
- LLM 输出异常时应有兜底规则。
- 向量化失败不应影响资源的基本入库和审核流程。

## 11. 技术选型建议

### 11.1 前端

- `React + TypeScript`：与现有项目形态更一致，迁移成本低。
- `React Flow`：适合课程图谱与学习路径可视化。
- `Tailwind CSS + shadcn/ui`：适合快速搭建一致风格界面。

### 11.2 后端

- `FastAPI`：适合快速定义接口、数据模型与文档。
- `Pydantic`：用于请求响应校验和生成结果校验。
- `SQLAlchemy` 或等价 ORM：用于数据建模和关系管理。

### 11.3 数据与检索

- `PostgreSQL`：统一承载结构化数据。
- `pgvector`：便于在同一数据库内完成早期向量检索闭环。
- `Redis`：用于邀请码、缓存和轻量异步状态管理。

### 11.4 智能能力

- 初期采用标准 LLM 接口即可，重点是把输入、输出和回退机制设计好。
- LangGraph 可作为第二阶段优化项，而不是第一阶段依赖项。

## 12. 风险与应对

### 12.1 路线图生成不稳定

风险：

- LLM 输出节点格式不稳定。
- 路线图内容与已有课程图谱冲突。

应对：

- 使用固定 JSON Schema 校验。
- 引入后端规则校验和默认模板回退。

### 12.2 文档解析质量不高

风险：

- 培养方案、实验指导中存在公式和复杂表格，解析结果质量不稳定。

应对：

- 优先支持 Markdown、网页和结构清晰的 PDF。
- 对复杂 PDF 单独设计人工清洗流程。

### 12.3 RAG 回答可信度不足

风险：

- 检索片段不准确导致回答失真。

应对：

- 强制返回来源。
- 设置最低召回阈值，低于阈值时明确提示无法可靠回答。

### 12.4 系统复杂度过早膨胀

风险：

- 过早引入多 Agent、复杂爬虫和过多第三方服务，导致开发难度和维护成本显著上升。

应对：

- 第一阶段采用单体后端 + 清晰模块边界。
- 第二阶段再按实际瓶颈决定是否拆分服务。

## 13. 分阶段实施方案

### 13.1 第一阶段：最小可用版本

目标：完成“课程图谱 + 资源管理 + 基础检索 + 路线图生成”闭环。

任务建议：

1. 建立 `CourseNode`、`CourseEdge`、`Resource` 等核心表。
2. 完成课程图谱和资源数据的手工初始化。
3. 搭建 Workspace 主页面。
4. 接入 React Flow 路线图页面与资源详情交互。
5. 提供学习路径生成接口并完成 Schema 校验。
6. 完成基础向量检索链路。
7. 提供简单的资源审核页。

### 13.2 第二阶段：增强智能能力

目标：提升平台自动化能力和回答质量。

任务建议：

1. 接入资源自动抓取与元数据提取。
2. 优化召回、重排和答案生成质量。
3. 引入更细粒度的用户状态与学习记录。
4. 逐步统一旧页面与新页面的视觉和组件体系。
5. 视复杂度引入 LangGraph 或任务编排框架。

### 13.3 第三阶段：平台化与运营化

目标：支持更长期的项目使用和扩展。

任务建议：

1. 完善权限与管理能力。
2. 支持更丰富的数据来源。
3. 引入数据分析、推荐与运营看板。

## 14. 第一阶段验收标准

第一阶段建议以如下结果作为验收依据：

- 能展示完整的课程节点与前置关系图。
- 能查看每个节点绑定的学习资源。
- 能提交资源并完成审核入库。
- 能根据用户输入生成一条结构化学习路径。
- 能基于站内资料完成带来源的检索问答。
- 核心流程能够稳定演示，不依赖人工临时修改数据库。

## 15. 结论

本项目的关键不在于一次性堆叠多少 AI 技术，而在于先把“课程图谱、资源管理、路径生成、检索问答”四个核心闭环做扎实。设计上应坚持“结构化数据优先、AI 能力后置增强、复杂架构按需引入”的原则。

如果后续需要继续完善，可以在此文档基础上补充两类配套文档：

- 接口设计文档。
- 数据库表结构与字段定义文档。
