# 测试指南

## 快速说明

> `.env` 和 `venv` 文件**没有被删除**，依然完整保留在 `backend/` 目录下。
> 之前的 git 操作只是让它们不会被上传到 GitHub（避免泄露 API Key）。

---

## 一、启动项目

### 方式1：一键启动（推荐）

```bash
cd /Users/limit/Desktop/zhinan
bash start.sh
```

### 方式2：分开启动

**后端**（新开一个终端）：
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000
# 首次启动约需 15s（加载 AI 向量模型）
# 看到 "Uvicorn running on http://0.0.0.0:8000" 即成功
```

**前端**（再开一个终端）：
```bash
cd frontend
npm run dev
# 看到 "Local: http://localhost:5173" 即成功
```

---

## 二、接口测试（curl 命令）

### 1. 健康检查
```bash
curl http://localhost:8000/health
# 期望: {"status":"healthy"}
```

### 2. 创建用户画像
```bash
curl -X POST http://localhost:8000/api/profiles \
  -H "Content-Type: application/json" \
  -d '{
    "current_knowledge": {
      "programming_languages": ["Python"],
      "completed_courses": ["CS50"],
      "skill_level": "beginner"
    },
    "learning_goals": {
      "target_skills": ["deep_learning", "computer_vision"],
      "specific_topics": ["CNN", "transformers"]
    },
    "career_direction": {
      "target_role": "AI工程师",
      "preferred_language": "Python",
      "industry": "tech"
    }
  }'
# 期望: {"id": 数字, "current_knowledge": {...}, ...}
# 记住返回的 id，下一步用
```

### 3. 获取推荐（首次约 7-8 秒）
```bash
# 把下面的 1 替换成上一步得到的 id
curl http://localhost:8000/api/recommendations/1
# 期望: 包含 5 条课程推荐的数组
```

### 4. 再次获取（命中缓存，约 30ms）
```bash
curl http://localhost:8000/api/recommendations/1
# 期望: 同上，但几乎瞬间返回
```

### 5. 强制刷新推荐
```bash
curl "http://localhost:8000/api/recommendations/1?refresh=true"
# 期望: 重新调用 LLM 生成新推荐理由（约 7-8 秒）
```

### 6. 查看 API 文档（自动生成）
浏览器打开：http://localhost:8000/docs

---

## 三、前端界面测试

打开浏览器访问：**http://localhost:5173**

### 测试流程
1. **第一步（当前基础）**：选择编程语言（如 Python）、技能水平（初学者）、已完成课程（可选）→ 点击"下一步"
2. **第二步（学习目标）**：选择目标技能（如 深度学习、计算机视觉）、具体主题（可选）→ 点击"下一步"
3. **第三步（职业方向）**：填写目标职位（如 AI工程师）、偏好语言（Python）、目标行业（科技）→ 点击"提交"
4. **等待结果**：首次约 **7-8 秒**返回 5 条 AI 推荐课程（含匹配度和推荐理由）

### 预期结果样例
- 推荐课程带有匹配分数（圆形进度条）
- 推荐理由由 DeepSeek AI 生成，针对你的画像定制
- 评分细节显示语言/难度/领域/前置知识四个维度

---

## 四、性能对比（优化前后）

| 场景 | 优化前 | 优化后 |
|---|---|---|
| 首次获取推荐 | ~20 秒 | **~7 秒** |
| 再次获取同一用户 | ~20 秒 | **~30 毫秒** |

---

## 五、常见问题

### Q: 后端启动慢（15秒以上）
正常现象。首次启动需要加载 AI 向量模型（`paraphrase-multilingual-MiniLM-L12-v2`）。之后每次请求都很快。

### Q: 推荐接口报 404"暂无课程数据"
数据库没有课程数据，需要初始化：
```bash
cd backend
source venv/bin/activate
python scripts/init_db.py
python scripts/init_vector_db.py
```

### Q: 推荐接口报 422 错误
表单有字段未填写。检查提交的 JSON 是否包含所有必填字段（`skill_level`、`target_skills`、`target_role`、`preferred_language`）。

### Q: LLM 不调用，推荐理由是模板格式
检查 `backend/.env` 中的 `LLM_API_KEY` 是否正确配置：
```bash
cat backend/.env | grep LLM_API_KEY
```

### Q: 团队成员如何配置（克隆 GitHub 仓库后）
`.env` 不会上传 GitHub，团队成员需手动创建：
```bash
cd backend
cp .env.example .env
# 编辑 .env，填入自己的 API Key
```
