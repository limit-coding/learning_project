# 🎓 AI学习指导系统 - 使用指南

## 项目简介

这是一个基于 AI 的个性化深度学习课程推荐系统，可以根据用户的学习画像智能推荐最适合的课程。

## 技术栈

- **后端**: FastAPI + SQLAlchemy + SQLite
- **前端**: React + TypeScript + Ant Design
- **AI**: DeepSeek API（可选）
- **推荐算法**: 多维度评分系统

## 快速开始

### 1. 启动系统

```bash
./start.sh
```

启动后访问：
- 前端界面: http://localhost:5173
- 后端 API: http://localhost:8000
- API 文档: http://localhost:8000/docs

### 2. 使用系统

1. 打开浏览器访问 http://localhost:5173
2. 填写三步用户画像：
   - **当前基础**: 编程语言、技能水平、已完成课程
   - **学习目标**: 目标技能、具体主题
   - **职业方向**: 目标职位、偏好语言、目标行业
3. 提交后系统会自动推荐最适合的课程
4. 查看推荐结果，包括：
   - 匹配度评分
   - 推荐理由
   - 课程详情
   - 各维度评分细节

## 推荐算法说明

系统使用 4 个维度评估课程匹配度（总分 100 分）：

1. **编程语言匹配** (30分)
   - 课程使用的编程语言与用户掌握的语言匹配度

2. **难度适配** (25分)
   - 课程难度与用户技能水平的匹配度

3. **领域相关度** (25分)
   - 课程主题与用户学习目标的相关度（Jaccard 相似度）

4. **前置知识满足度** (20分)
   - 用户已完成的课程是否满足课程前置要求

## 配置说明

### 数据库

系统默认使用 SQLite，数据库文件位于 `backend/zhinan.db`

### DeepSeek API（可选）

如需使用 AI 生成个性化推荐理由：

1. 访问 https://platform.deepseek.com/ 注册并获取 API Key
2. 编辑 `backend/.env` 文件
3. 填入 `DEEPSEEK_API_KEY=your_api_key_here`
4. 重启后端服务

**注意**: 没有 API Key 也能正常运行，系统会使用模板化推荐理由。

## 添加课程数据

编辑 `data/courses_seed.json` 文件添加新课程，然后重新初始化数据库：

```bash
cd backend
source venv/bin/activate
python scripts/init_db.py
```

## 项目结构

```
zhinan/
├── backend/              # 后端代码
│   ├── app/
│   │   ├── main.py      # FastAPI 应用入口
│   │   ├── config.py    # 配置管理
│   │   ├── database.py  # 数据库连接
│   │   ├── models/      # 数据模型
│   │   ├── schemas/     # Pydantic schemas
│   │   ├── api/         # API 路由
│   │   └── services/    # 业务逻辑
│   ├── scripts/         # 工具脚本
│   ├── venv/            # Python 虚拟环境
│   ├── .env             # 环境变量配置
│   └── requirements.txt # Python 依赖
├── frontend/            # 前端代码
│   ├── src/
│   │   ├── App.tsx      # 主应用组件
│   │   ├── components/  # React 组件
│   │   ├── services/    # API 调用
│   │   └── types/       # TypeScript 类型
│   ├── package.json     # 前端依赖
│   └── vite.config.ts   # Vite 配置
├── data/                # 数据文件
│   └── courses_seed.json # 课程种子数据
├── start.sh             # 启动脚本
└── README.md            # 项目文档
```

## 常见问题

### 端口被占用

如果 8000 或 5173 端口被占用：

```bash
# 查找并停止占用端口的进程
lsof -ti:8000 | xargs kill -9
lsof -ti:5173 | xargs kill -9
```

### 重新初始化数据库

```bash
cd backend
rm zhinan.db  # 删除旧数据库
source venv/bin/activate
python scripts/init_db.py
```

### 重新安装依赖

```bash
# 后端
cd backend
rm -rf venv
/opt/anaconda3/bin/python3.12 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 前端
cd frontend
rm -rf node_modules package-lock.json
npm install
```

## 系统特点

✅ 无需用户注册登录（简化版）
✅ 直接填写画像获取推荐
✅ 聚焦深度学习领域
✅ 使用 DeepSeek（国内 API）
✅ 多维度智能推荐算法
✅ 响应式界面设计
✅ 完整的 API 文档

## 开发说明

### 后端开发

```bash
cd backend
source venv/bin/activate
python -m app.main  # 启动开发服务器
```

访问 http://localhost:8000/docs 查看 API 文档

### 前端开发

```bash
cd frontend
npm run dev  # 启动开发服务器
```

访问 http://localhost:5173

## 许可证

MIT License

## 联系方式

如有问题或建议，欢迎提 Issue！
