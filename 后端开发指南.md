# 🎓 后端开发完整指南

## 📋 项目概述

你负责的后端部分已经完成并成功运行，包括：
- FastAPI 框架
- SQLite 数据库
- 推荐算法引擎
- 多模型 LLM 集成
- RAG 向量检索增强

---

## 🚀 快速启动

### 启动项目
```bash
# 方式1：自动启动（推荐）
cd /Users/limit/Desktop/zhinan
./start.sh

# 方式2：手动启动后端
cd /Users/limit/Desktop/zhinan/backend
source venv/bin/activate
python -m app.main
```

### 访问地址
- 后端 API: http://localhost:8000
- API 文档: http://localhost:8000/docs
- 前端界面: http://localhost:5173

---

## 📁 后端文件结构

```
backend/
├── app/
│   ├── main.py                    # 应用入口
│   ├── config.py                  # 配置管理
│   ├── database.py                # 数据库连接
│   ├── api/
│   │   └── routes.py              # API 路由
│   ├── models/
│   │   └── models.py              # 数据模型
│   ├── schemas/
│   │   └── schemas.py             # 数据验证
│   └── services/
│       ├── recommendation_engine.py  # 推荐算法
│       ├── llm_service.py            # LLM 服务
│       └── vector_service.py         # 向量数据库
├── scripts/
│   ├── init_db.py                 # 数据库初始化
│   └── init_vector_db.py          # 向量库初始化
├── .env                           # 环境变量
├── requirements.txt               # 依赖包
└── zhinan.db                      # SQLite 数据库
```

---

## 🔧 核心技术栈

### 1. FastAPI (Web 框架)
- 自动生成 API 文档
- 类型检查和验证
- 异步支持

### 2. SQLAlchemy (ORM)
- 数据库抽象层
- 模型定义
- 查询构建

### 3. Pydantic (数据验证)
- 请求/响应验证
- 类型安全
- 自动文档生成

### 4. ChromaDB + Sentence Transformers (RAG)
- 向量数据库
- 语义搜索
- 课程内容检索

### 5. LLM API (AI 集成)
- 支持 DeepSeek/OpenAI/自定义
- 个性化推荐理由生成
- 降级方案

---

## 📝 API 接口说明

### 1. 创建用户画像
```http
POST /api/profiles
Content-Type: application/json

{
  "current_knowledge": {
    "programming_languages": ["Python"],
    "skill_level": "intermediate",
    "completed_courses": []
  },
  "learning_goals": {
    "target_skills": ["deep_learning"],
    "specific_topics": ["CNN"]
  },
  "career_direction": {
    "target_role": "AI工程师",
    "preferred_languages": ["Python"],
    "target_industry": "科技"
  }
}
```

### 2. 获取用户画像
```http
GET /api/profiles/{profile_id}
```

### 3. 获取推荐课程
```http
GET /api/recommendations/{profile_id}?top_n=5
```

---

## 🧮 推荐算法详解

### 评分维度（总分100）

**1. 编程语言匹配 (30分)**
```python
# 用户掌握的语言 vs 课程使用的语言
if 课程语言 in 用户语言:
    得分 = 30
elif 课程语言为通用语言:
    得分 = 15
else:
    得分 = 0
```

**2. 难度适配 (25分)**
```python
# 用户水平 vs 课程难度
if 完全匹配:
    得分 = 25
elif 相差一级:
    得分 = 15
else:
    得分 = 5
```

**3. 领域相关度 (25分)**
```python
# 用户目标 vs 课程主题
匹配主题数 / 总主题数 * 25
```

**4. 前置知识满足度 (20分)**
```python
# 用户已完成课程 vs 课程前置要求
满足的前置数 / 总前置数 * 20
```

---

## 🤖 AI 集成说明

### 配置 API Key

编辑 `backend/.env`:
```bash
LLM_PROVIDER=deepseek
LLM_API_KEY=sk-your-key-here
LLM_MODEL=deepseek-chat
```

### 支持的模型

**DeepSeek (推荐)**
```bash
LLM_PROVIDER=deepseek
LLM_API_KEY=sk-xxx
LLM_MODEL=deepseek-chat
```

**OpenAI**
```bash
LLM_PROVIDER=openai
LLM_API_KEY=sk-xxx
LLM_MODEL=gpt-4
```

**自定义 API**
```bash
LLM_PROVIDER=custom
LLM_API_KEY=your-key
LLM_MODEL=your-model
CUSTOM_API_BASE=https://your-api.com/v1
```

### 工作流程

1. 用户请求推荐
2. 推荐算法计算匹配度
3. 向量库检索课程详细信息
4. LLM 基于真实数据生成推荐理由
5. 返回结果给前端

---

## 🔍 RAG 增强功能

### 初始化向量数据库
```bash
cd backend
source venv/bin/activate
python scripts/init_vector_db.py
```

### 工作原理

1. **索引阶段**
   - 读取 20 门课程数据
   - 使用多语言模型生成向量
   - 存储到 ChromaDB

2. **检索阶段**
   - 根据课程代码检索
   - 获取完整课程描述
   - 注入到 LLM prompt

3. **生成阶段**
   - LLM 基于真实内容
   - 生成个性化推荐理由
   - 避免幻觉问题

---

## 🎯 学习路径建议

### 第1周：基础理解
1. 阅读 `main.py` - 理解应用入口
2. 阅读 `routes.py` - 理解 API 设计
3. 阅读 `models.py` - 理解数据模型
4. 测试所有 API 接口

### 第2周：核心逻辑
1. 阅读 `recommendation_engine.py` - 理解推荐算法
2. 修改评分权重，观察效果
3. 添加新的评分维度

### 第3周：AI 集成
1. 阅读 `llm_service.py` - 理解 LLM 调用
2. 阅读 `vector_service.py` - 理解 RAG
3. 测试不同模型效果

### 第4周：功能扩展
1. 添加新的 API 端点
2. 优化推荐算法
3. 实现用户反馈功能

---

## 🛠 常用命令

### 数据库操作
```bash
# 重新初始化数据库
cd backend
rm zhinan.db
python scripts/init_db.py

# 查看数据库内容
sqlite3 zhinan.db
.tables
SELECT * FROM courses LIMIT 5;
.quit
```

### 依赖管理
```bash
# 安装新依赖
pip install package_name
pip freeze > requirements.txt

# 更新依赖
pip install --upgrade package_name
```

### 测试 API
```bash
# 使用 curl 测试
curl http://localhost:8000/api/profiles/1

# 或访问自动文档
open http://localhost:8000/docs
```

---

## 📊 性能优化建议

1. **数据库查询优化**
   - 添加索引
   - 使用查询缓存
   - 减少 N+1 查询

2. **LLM 调用优化**
   - 实现结果缓存
   - 批量处理请求
   - 设置合理超时

3. **向量检索优化**
   - 预加载常用向量
   - 使用更快的模型
   - 实现分页查询

---

## 🐛 常见问题

### 1. 端口被占用
```bash
lsof -ti:8000 | xargs kill -9
```

### 2. 依赖安装失败
```bash
pip install --upgrade pip
pip install -r requirements.txt --no-cache-dir
```

### 3. 向量库初始化慢
首次运行会下载模型（约120MB），之后会很快

### 4. LLM 调用失败
检查 API Key 是否正确，或使用降级方案（模板化推荐）

---

## 📚 推荐学习资源

1. **FastAPI 官方文档**: https://fastapi.tiangolo.com/
2. **SQLAlchemy 教程**: https://docs.sqlalchemy.org/
3. **Pydantic 文档**: https://docs.pydantic.dev/
4. **ChromaDB 文档**: https://docs.trychroma.com/
5. **DeepSeek API**: https://platform.deepseek.com/

---

## ✅ 已完成功能清单

- [x] FastAPI 应用框架
- [x] SQLite 数据库集成
- [x] 用户画像 CRUD
- [x] 4维度推荐算法
- [x] 多模型 LLM 支持
- [x] RAG 向量检索
- [x] API 自动文档
- [x] CORS 跨域配置
- [x] 错误处理和降级

---

## 🚀 未来扩展方向

### 短期（1-2周）
1. 添加用户认证系统
2. 实现课程搜索功能
3. 添加学习进度跟踪

### 中期（1个月）
1. 优化推荐算法（协同过滤）
2. 实现课程评价功能
3. 添加数据分析统计

### 长期（2-3个月）
1. 实现学习路径规划
2. 添加社区讨论功能
3. Docker 容器化部署

---

**文档更新时间**: 2026-03-09
**项目状态**: ✅ 运行中
**后端负责人**: 你
