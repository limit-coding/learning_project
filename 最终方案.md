# AI 学习资源站最终方案

## 1. 方案定位

最终方案采用“现有推荐系统为基础，逐步升级为 AI 学习资源站”的路线。

这意味着：

- 保留现有项目中已经完成的用户画像、课程推荐、前后端分离和 LLM 推荐理由生成能力。
- 将“深度学习课程推荐系统”重新定义为平台第一阶段中的一个子能力，而不是整个系统的最终形态。
- 平台长期目标不再局限于深度学习课程推荐，而是扩展为面向计算机与通信方向的课程图谱、资源管理、路径生成和资料检索平台。

## 2. 两份文档的融合结论

### 2.1 你同学文档的价值

你同学写的技术文档有两个明显优点：

- 很贴近当前已完成系统，能反映真实实现情况。
- 对模块、接口、运行环境、性能目标和存储方式描述得比较具体。

它适合作为“当前版本实现说明”或“阶段性技术说明书”。

### 2.2 当前设计文档的价值

当前设计文档的优势在于：

- 明确了系统长期目标和边界。
- 把平台拆成路线图、资源管理、路径生成、RAG 检索四个核心闭环。
- 更适合指导后续重构、数据库演进和功能扩展。

它适合作为“未来版本架构设计文档”。

### 2.3 融合后的判断

最终不应二选一，而应采用“双层定义”：

- 你同学那份文档描述“当前已实现的 MVP”。
- 设计文档描述“系统下一步要演进到的目标架构”。

因此，最终方案不是推翻现有实现重做，而是在现有系统上做演进式重构。

## 3. 最终产品定义

最终产品名称建议统一为：

`AI 学习资源站`

产品定位：

- 面向计算机与通信方向学生的学习资源与学习路径平台。
- 以结构化课程图谱为骨架。
- 以个性化推荐和学习路径生成为智能入口。
- 以 RAG 检索问答为资料使用入口。

产品内的“课程推荐系统”保留，但定位调整为：

- 平台中的一个功能模块。
- 主要负责根据用户画像或学习目标推荐课程与资源。

## 4. 最终功能范围

### 4.1 第一阶段必须完成

第一阶段建议统一为以下 5 个能力：

1. 用户画像与目标录入。
2. 课程与资源的结构化管理。
3. 基于规则和画像的推荐能力。
4. 学习路径生成能力。
5. 基于站内资料的基础检索问答能力。

### 4.2 第一阶段可以保留但不作为主线

以下功能可以继续保留，但不应占据系统主线：

- 收藏课程。
- 学习历史。
- 简单登录。
- 本地缓存推荐结果。

这些功能更多属于“使用体验增强”，优先级低于课程图谱、资源库和检索闭环。

### 4.3 第二阶段再做

- 资源提交和审核工作流完善。
- 自动抓取和元数据提取。
- 更完整的权限体系。
- 更细粒度的学习进度管理。
- 更强的 RAG 重排与评估。

## 5. 最终技术路线

### 5.1 总体原则

采用“兼容现有实现、逐步替换关键部件”的方式推进，不进行激进重写。

### 5.2 前端方案

前端最终建议继续使用：

- `React + TypeScript`

前端采用“渐进式重构”策略，而不是全量推翻重写。

这意味着：

- 保留现有 `React + TypeScript` 基础工程和能复用的业务页面。
- 将前端升级重点放在核心产品形态，而不是优先做整站组件库迁移。
- 新页面按目标形态设计，旧页面在短期内允许继续存在。

第一阶段 UI 策略：

- 现有页面能复用的继续复用。
- 课程推荐页保留，但逐步弱化其“独立系统”定位。
- 新增 `Workspace` 主页面，作为后续核心入口。
- 逐步新增路线图页、资源详情页、检索问答页。

前端交互形态建议：

- 采用类似 Claude / Vercel 的 Workspace 结构。
- 左侧区域负责目标输入、对话流或操作流。
- 右侧区域负责展示结构化结果，例如 Roadmap、资源详情和生成结果。
- 路线图区域使用 `React Flow` 实现课程图谱与路径展示。
- 节点点击后展示资源详情面板，可采用右侧固定详情区或 Drawer。

组件与视觉建议：

- 现有 Ant Design 页面已经完成的部分，可以短期继续使用。
- 新增核心页面建议优先采用 `Tailwind CSS + shadcn/ui`。
- 旧页面不要求第一阶段立即迁移，但后续新增页面应尽量统一到新风格。
- 因此前端允许在阶段内存在 Ant Design 与 `shadcn/ui` 并存的过渡状态。

第一阶段结论：

- 不建议为了风格统一而立刻重写全站前端组件库。
- 建议采用“老页面保留、新页面按新方案建设”的方式推进。
- 核心目标是先做出可用且具有产品感的 Workspace、Roadmap 和资源详情体验。

第二阶段前端演进方向：

- 根据第一阶段页面沉淀情况，逐步替换老旧 Ant Design 页面。
- 统一颜色、排版、卡片、表单和侧栏交互风格。
- 最终再决定是否彻底完成组件体系迁移。

### 5.3 后端方案

后端继续使用：

- `FastAPI`

原因：

- 与现有项目一致。
- 易于扩展 REST API。
- 适合承载推荐、路径生成、资源管理和 RAG 服务。

智能编排策略：

- 第一阶段不把 LangGraph 作为核心依赖。
- 先用普通服务层实现推荐、路径生成和 RAG。
- 等流程复杂后，再引入图编排框架。

### 5.4 数据库方案

数据库采用分阶段方案：

- 开发与演示环境：可暂时保留 SQLite。
- 第一阶段正式版本：迁移到 PostgreSQL。
- 需要向量检索时：启用 pgvector。

这样设计的原因：

- 现有代码已经基于 SQLite 跑通，适合继续开发。
- 最终目标包含课程图谱、资源关系、文档切片和向量检索，SQLite 迟早会成为瓶颈。

结论：

- SQLite 不是错误，但只能作为过渡。
- PostgreSQL + pgvector 是最终目标方案。

### 5.5 LLM 方案

LLM 在最终方案中承担 3 类任务：

1. 生成推荐理由。
2. 生成学习路径。
3. 基于检索结果组织回答。

设计原则：

- LLM 不直接负责核心业务真值。
- 课程依赖关系、资源状态和审核结论应以结构化数据为准。
- 所有生成结果都要经过 Schema 校验或后端规则校验。

## 6. 最终架构方案

系统采用前后端分离 + 单体后端 + 可扩展智能服务的架构。

### 6.1 前端层

负责：

- 用户画像填写。
- 推荐结果展示。
- 路线图可视化。
- 资源详情展示。
- 检索问答交互。
- 管理审核页面。

### 6.2 后端应用层

负责：

- 用户画像接口。
- 课程与资源接口。
- 推荐接口。
- 路线图生成接口。
- 检索问答接口。
- 审核接口。

### 6.3 智能服务层

负责：

- 推荐理由生成。
- 学习目标解析。
- 路径生成。
- RAG 回答生成。

### 6.4 数据层

负责：

- 用户画像。
- 课程节点与前置关系。
- 资源与资源审核状态。
- 文档切片与向量。
- 推荐记录与操作日志。

## 7. 最终模块划分

### 7.1 保留现有模块

- 用户画像模块。
- 推荐引擎模块。
- LLM 推荐理由模块。
- 前端表单模块。
- 推荐结果展示模块。

### 7.2 新增核心模块

- 课程图谱模块。
- 资源管理模块。
- 路径生成模块。
- 文档切片与向量化模块。
- RAG 检索问答模块。
- 管理审核模块。

### 7.3 降级处理的模块

以下模块先不作为主架构重点：

- 完整认证系统。
- 学习历史系统。
- 收藏系统。

可以保留现状，但不应优先占用主要开发资源。

## 8. 最终数据模型

最终建议数据库核心实体统一为：

- `User`
- `UserProfile`
- `CourseNode`
- `CourseEdge`
- `Resource`
- `ResourceCourseMapping`
- `RecommendationRecord`
- `DocumentChunk`
- `Embedding`
- `ReviewLog`

说明：

- 你同学文档中的 `courses`、`recommendations`、`user_profiles` 是当前推荐系统所需的最小模型。
- 在最终方案中，这些表应向更通用的课程图谱和资源系统演进。

## 9. 最终接口方案

第一阶段推荐保留并扩展以下接口。

### 9.1 现有接口保留

- `POST /api/profiles`
- `GET /api/profiles/{id}`
- `GET /api/recommendations/{id}`

### 9.2 新增接口

- `GET /api/course-nodes`
- `GET /api/course-edges`
- `GET /api/resources`
- `GET /api/resources/{id}`
- `POST /api/resources/submit`
- `POST /api/resources/{id}/review`
- `POST /api/roadmaps/generate`
- `POST /api/search`
- `POST /api/chat/retrieve`

结论：

- 现有推荐接口不废弃。
- 现有接口作为平台智能入口的一部分继续服务。

## 10. 路线选择上的关键决策

### 10.1 不重做，做演进式重构

原因：

- 现有系统已经有真实代码和业务闭环。
- 直接推翻重做风险高，且会浪费已有成果。

### 10.2 不把“深度学习课程推荐”当成最终产品边界

原因：

- 这个范围太窄，不足以支撑“AI 学习资源站”的长期目标。
- 但它适合作为第一阶段中已经完成的一个高价值功能。

### 10.3 不在第一阶段强依赖 LangGraph

原因：

- 当前流程复杂度不够高。
- 使用服务层和清晰接口即可完成主要需求。

### 10.4 数据库必须预留迁移到 PostgreSQL 的路线

原因：

- 最终要支持课程图谱关系、资源审核和向量检索。
- 继续长期依赖 SQLite 会限制系统演进。

### 10.5 前端采用“核心体验重做，基础工程保留”的策略

原因：

- 现有前端中已有表单、推荐展示和 API 对接逻辑，直接丢弃会造成浪费。
- 平台最终产品形态又确实需要 Workspace、Roadmap 和资源详情这些更强的交互结构。
- 因此最合理的方式不是“不改前端”，也不是“全站重写”，而是只重做最关键的用户体验层。

## 11. 最终实施路线

### 阶段 A：整理现有系统

目标：

- 统一命名。
- 清理文档。
- 明确当前代码和目标架构的对应关系。
- 明确前端哪些页面保留、哪些页面重做。

任务：

1. 将系统名称统一为 `AI 学习资源站`。
2. 将“课程推荐系统”调整为平台中的一个模块描述。
3. 整理现有 API、数据表和前端页面。
4. 划分前端页面为“保留页”“过渡页”“新建设页”。

### 阶段 B：补齐平台骨架

目标：

- 在现有系统上补齐课程图谱与资源系统。
- 建立新的前端 Workspace 形态。

任务：

1. 增加 `CourseNode` 和 `CourseEdge`。
2. 增加 `Resource` 和资源绑定关系。
3. 新建 `Workspace` 主页面。
4. 接入 `React Flow` 路线图视图。
5. 新增资源展示与管理页。
6. 新增学习路径生成接口。

### 阶段 C：接入 RAG

目标：

- 让平台从“推荐系统”升级为“资源检索平台”。
- 让 Workspace 成为统一交互入口。

任务：

1. 接入文档切片和向量化。
2. 建立基础召回链路。
3. 提供带来源的问答接口。
4. 在前端接入左侧输入区与右侧结果展示区。

### 阶段 D：完善审核与运营能力

目标：

- 支持长期维护和资源增长。

任务：

1. 资源提交与审核。
2. 操作日志与审核记录。
3. 用户反馈与推荐优化。

## 12. 最终结论

最终方案应明确为：

- 以现有“个性化课程推荐系统”为基础版本。
- 通过演进式重构，升级为“AI 学习资源站”。
- 技术上继续采用 `FastAPI + React + TypeScript`。
- 数据库从 `SQLite` 过渡到 `PostgreSQL + pgvector`。
- 第一阶段以课程图谱、资源管理、推荐能力和路径生成为主。
- RAG 检索问答作为平台升级的关键能力，在第二阶段稳步接入。

这个方案的核心优点是：

- 不浪费现有成果。
- 目标比当前实现更完整。
- 技术复杂度可控。
- 能同时满足课程设计展示和后续真实扩展。
