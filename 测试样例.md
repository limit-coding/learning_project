# 测试样例说明

## 🎯 推荐的测试样例

### 样例 1：初学者 - 深度学习入门

**第一步 - 当前基础：**
- 编程语言：选择 `Python`
- 技能水平：选择 `初学者`
- 已完成课程：输入 `CS50`（可选，可以不填）

**第二步 - 学习目标：**
- 目标技能：选择 `深度学习`
- 具体主题：输入 `CNN`（可选）

**第三步 - 职业方向：**
- 目标职位：输入 `AI工程师`
- 偏好语言：选择 `Python`
- 目标行业：选择 `科技`

**预期结果：** 系统会推荐 fast.ai、MIT 6.S191 等适合初学者的深度学习课程

---

### 样例 2：中级开发者 - 计算机视觉

**第一步 - 当前基础：**
- 编程语言：选择 `Python` 和 `C++`
- 技能水平：选择 `中级`
- 已完成课程：输入 `Andrew Ng ML`

**第二步 - 学习目标：**
- 目标技能：选择 `计算机视觉` 和 `深度学习`
- 具体主题：输入 `CNN`, `YOLO`

**第三步 - 职业方向：**
- 目标职位：输入 `计算机视觉工程师`
- 偏好语言：选择 `Python`
- 目标行业：选择 `科技`

**预期结果：** 系统会推荐 Stanford CS231n、YOLO 课程等计算机视觉专业课程

---

### 样例 3：高级开发者 - NLP 研究

**第一步 - 当前基础：**
- 编程语言：选择 `Python`
- 技能水平：选择 `高级`
- 已完成课程：输入 `Deep Learning Specialization`

**第二步 - 学习目标：**
- 目标技能：选择 `自然语言处理` 和 `生成式AI`
- 具体主题：输入 `transformers`, `BERT`

**第三步 - 职业方向：**
- 目标职位：输入 `NLP研究员`
- 偏好语言：选择 `Python`
- 目标行业：选择 `学术研究`

**预期结果：** 系统会推荐 Stanford CS224n、Hugging Face NLP Course 等 NLP 高级课程

---

## 🔑 DeepSeek API Key 申请（可选）

### 为什么需要 API Key？
- ✅ **不需要也能用**：没有 API Key，系统使用模板化推荐理由
- ✅ **有了更智能**：有 API Key 后，推荐理由会更个性化和详细

### 申请步骤：

1. **访问官网**
   - 打开 https://platform.deepseek.com/
   - 点击右上角"注册"

2. **注册账号**
   - 使用邮箱或手机号注册
   - 完成验证

3. **创建 API Key**
   - 登录后进入"控制台"
   - 左侧菜单点击 "API Keys"
   - 点击"创建新的 API Key"
   - 复制生成的 Key（格式：sk-xxxxxxxxxx）

4. **配置到项目**
   ```bash
   # 编辑配置文件
   cd /Users/limit/Desktop/zhinan/backend
   nano .env

   # 修改这一行（去掉注释并填入你的 Key）
   DEEPSEEK_API_KEY=sk-你的API密钥

   # 保存并退出（Ctrl+O, Enter, Ctrl+X）
   ```

5. **重启后端**
   ```bash
   # 停止当前运行的后端（如果有）
   # 然后重新启动
   cd backend
   source venv/bin/activate
   python -m app.main
   ```

### 费用说明：
- 新用户通常有免费额度
- DeepSeek 价格相对便宜
- 本项目调用量很小，基本不会产生费用

---

## 🐛 常见问题排查

### 问题 1：提交后显示"创建失败"

**可能原因：**
1. 后端服务未启动
2. 必填字段未填写完整
3. 网络连接问题

**解决方法：**
```bash
# 1. 检查后端是否运行
curl http://localhost:8000/health
# 应该返回：{"status":"healthy"}

# 2. 查看后端日志
cd /Users/limit/Desktop/zhinan/backend
tail -f /tmp/backend.log

# 3. 重启后端
source venv/bin/activate
python -m app.main
```

### 问题 2：前端无法访问

**解决方法：**
```bash
# 检查前端是否运行
curl http://localhost:5173

# 重启前端
cd /Users/limit/Desktop/zhinan/frontend
npm run dev
```

### 问题 3：端口被占用

**解决方法：**
```bash
# 停止占用 8000 端口的进程
lsof -ti:8000 | xargs kill -9

# 停止占用 5173 端口的进程
lsof -ti:5173 | xargs kill -9
```

---

## 📊 推荐算法说明

系统使用 4 个维度评分（总分 100）：

1. **编程语言匹配 (30分)**
   - 课程使用的语言 ∈ 你掌握的语言 → 满分
   - 否则 → 0分

2. **难度适配 (25分)**
   - 课程难度 = 你的技能水平 → 满分
   - 相差一级 → 15分
   - 相差两级 → 0分

3. **领域相关度 (25分)**
   - 使用 Jaccard 相似度计算
   - 课程主题 ∩ 你的学习目标 / 课程主题 ∪ 你的学习目标

4. **前置知识满足度 (20分)**
   - 你已完成的课程 ⊇ 课程前置要求 → 满分
   - 按比例计算

**最终匹配度 = 四个维度得分之和**

---

## 🎓 系统特点

- ✅ 无需注册登录
- ✅ 3 步完成画像填写
- ✅ 智能多维度推荐
- ✅ 20 门精选课程
- ✅ 详细的评分细节
- ✅ 可选的 AI 增强

---

## 📞 需要帮助？

1. 查看 [使用指南.md](使用指南.md)
2. 查看 [项目完成总结.md](项目完成总结.md)
3. 访问 API 文档：http://localhost:8000/docs
