# 🐛 故障排除指南

## 问题：提交表单显示"创建失败，请重试"

### ✅ 系统已修复！

**问题原因**：后端服务未启动

**解决方案**：后端已重新启动，系统现在应该可以正常工作了。

---

## 📋 快速检查清单

运行诊断脚本：
```bash
cd /Users/limit/Desktop/zhinan
./diagnose.sh
```

所有项目都应该显示 ✅

---

## 🔍 如果仍然失败，请按以下步骤排查

### 步骤 1：打开浏览器开发者工具

1. 打开浏览器访问 http://localhost:5173
2. 按 `F12` 或右键点击页面 → "检查"
3. 切换到 **Console** 标签
4. 尝试提交表单
5. 查看红色错误信息

### 步骤 2：常见错误及解决方法

#### 错误 1：`Network Error` 或 `ERR_CONNECTION_REFUSED`
**原因**：后端未运行

**解决**：
```bash
cd /Users/limit/Desktop/zhinan/backend
source venv/bin/activate
python -m app.main
```

#### 错误 2：`404 Not Found`
**原因**：API 路径错误或前端代理配置问题

**解决**：重启前端
```bash
cd /Users/limit/Desktop/zhinan/frontend
# 按 Ctrl+C 停止当前进程
npm run dev
```

#### 错误 3：`CORS policy` 错误
**原因**：跨域配置问题

**解决**：重启后端
```bash
# 停止后端进程
lsof -ti:8000 | xargs kill -9

# 重新启动
cd /Users/limit/Desktop/zhinan/backend
source venv/bin/activate
python -m app.main
```

#### 错误 4：`500 Internal Server Error`
**原因**：后端代码错误或数据库问题

**解决**：查看后端日志
```bash
# 查看后端日志
tail -50 /tmp/backend.log

# 或者在前台运行后端查看实时日志
cd /Users/limit/Desktop/zhinan/backend
source venv/bin/activate
python -m app.main
```

#### 错误 5：字段验证错误
**原因**：必填字段未填写

**解决**：确保填写所有必填项：
- 第一步：编程语言、技能水平（必填）
- 第二步：目标技能（必填）
- 第三步：目标职位、偏好语言（必填）

---

## 🧪 手动测试 API

### 测试 1：检查后端健康状态
```bash
curl http://localhost:8000/health
# 应该返回：{"status":"healthy"}
```

### 测试 2：创建用户画像
```bash
curl -X POST http://localhost:8000/api/profiles \
  -H "Content-Type: application/json" \
  -d '{
    "current_knowledge": {
      "programming_languages": ["Python"],
      "completed_courses": [],
      "skill_level": "beginner"
    },
    "learning_goals": {
      "target_skills": ["deep_learning"],
      "specific_topics": []
    },
    "career_direction": {
      "target_role": "AI工程师",
      "preferred_language": "Python",
      "industry": "tech"
    }
  }'
```

如果返回包含 `"id":` 的 JSON，说明 API 工作正常。

### 测试 3：获取推荐
```bash
# 使用上一步返回的 id（假设是 1）
curl http://localhost:8000/api/recommendations/1?top_n=3
```

---

## 🔄 完全重启系统

如果以上都不行，尝试完全重启：

```bash
# 1. 停止所有服务
lsof -ti:8000 | xargs kill -9
lsof -ti:5173 | xargs kill -9

# 2. 等待 2 秒
sleep 2

# 3. 使用启动脚本重新启动
cd /Users/limit/Desktop/zhinan
./start.sh
```

---

## 📸 截图示例

### 正确的表单填写示例

**第一步 - 当前基础：**
```
编程语言: [Python] ✓ 必填
技能水平: 初学者 ✓ 必填
已完成课程: CS50 (可选)
```

**第二步 - 学习目标：**
```
目标技能: [深度学习] ✓ 必填
具体主题: CNN (可选)
```

**第三步 - 职业方向：**
```
目标职位: AI工程师 ✓ 必填
偏好语言: Python ✓ 必填
目标行业: 科技 (默认)
```

---

## 🆘 仍然无法解决？

### 收集诊断信息

1. **运行诊断脚本**
```bash
cd /Users/limit/Desktop/zhinan
./diagnose.sh > diagnosis.txt
```

2. **查看后端日志**
```bash
tail -100 /tmp/backend.log > backend_log.txt
```

3. **浏览器控制台截图**
- 打开 F12 开发者工具
- Console 标签的错误信息
- Network 标签的失败请求

4. **检查端口占用**
```bash
lsof -i:8000
lsof -i:5173
```

---

## ✅ 验证系统正常工作

成功的标志：

1. ✅ 访问 http://localhost:5173 能看到表单
2. ✅ 填写表单后点击"提交"
3. ✅ 看到"画像创建成功！"的提示
4. ✅ 页面自动跳转到推荐结果
5. ✅ 显示课程推荐卡片

---

## 💡 预防措施

为了避免再次出现问题：

1. **使用启动脚本**
   ```bash
   ./start.sh
   ```
   而不是手动启动

2. **不要关闭终端窗口**
   启动后保持终端窗口打开

3. **检查端口占用**
   启动前确保 8000 和 5173 端口未被占用

4. **定期检查**
   ```bash
   ./diagnose.sh
   ```

---

## 📞 联系支持

如果问题持续存在，请提供：
- `./diagnose.sh` 的输出
- 浏览器 Console 的错误信息
- `/tmp/backend.log` 的最后 50 行
